import { ComponentType, DComponent, MetadataStorage } from "../../index.js";
export function ModalComponent(id, params) {
    return function (target, key) {
        const button = DComponent.create(ComponentType.Modal, id ?? key, params?.guilds, params?.botIds).decorate(target.constructor, key, target[key]);
        MetadataStorage.instance.addComponentModal(button);
    };
}
//# sourceMappingURL=ModalComponent.js.map