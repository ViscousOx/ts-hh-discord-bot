import { DOn, MetadataStorage } from "../../index.js";
export function On(event, options) {
    return function (target, key, descriptor) {
        const clazz = target;
        const on = DOn.create(event, false, options?.botIds).decorate(clazz.constructor, key, descriptor?.value);
        MetadataStorage.instance.addOn(on);
    };
}
//# sourceMappingURL=On.js.map